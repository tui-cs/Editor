// Placeholder so the assembly compiles to a non-empty target during repo bootstrap.
// Replace with real types as Phase 2+ (rendering, caret, selection) lands per /specs/00-plan.md.

namespace Terminal.Gui.Views;

public static class AssemblyMarker
{
}
