// Placeholder so the assembly compiles to a non-empty target during repo bootstrap.
// Replace with real types as Phase 1 (Document/, Utils/) lands per /specs/00-plan.md.

namespace Terminal.Gui.Text;

public static class AssemblyMarker
{
}
